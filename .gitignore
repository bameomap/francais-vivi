node_modules
dist
.env
.env.local
.DS_Store
.vercel
.claude

Nouvel_Edito_A1_audios_manuel/
public/audio
.env*.local
french.png

# materials/ — reference PDFs & audio, kept local only (some files >100MB,
# over GitHub's push limit; see materials/README.md)
EDITO A2/
Sujet DELF A1/
A1 Cahier/
Edito A1 by Unite md/
Nouvel_Edito_A1_audios_cahier/
Guide-pedagogique-Edito-A1-2022.pdf

# Alignment scratch: downloaded mp3s, exported transcripts, the venv.
# Only the generated src/data/editoTimingsA2.js is committed.
scripts/align-audio/work/
scripts/align-audio/.venv

# Source book for the DELF A1 réussite section — scans + audio, regenerate crops
# with scripts/delf-a1-crop.py rather than committing ~100 MB of originals.
materials/100% DELF Level/
delf-a1-contact-sheet.png
.env*
